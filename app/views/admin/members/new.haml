%section
    .page-header
        %h2
            Add Member
            %small
                %a{:href => admin_group_members_path(@group), :style => "color: " + @group.html_color_css}= @group.name
    .row
        .col-md-6
            = render partial: 'form', locals: {url: admin_group_members_path(@group)}
