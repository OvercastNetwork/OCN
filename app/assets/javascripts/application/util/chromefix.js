$(document).ready(function () {
    // tempory hack fix until chrome resolves issues their stupid font rendering
    // issues that are still present, even though they are reported as fixed
    $('body').hide().show();
});
